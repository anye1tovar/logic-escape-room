PORT=4000
DATABASE_FILE=./data/bookings.sqlite

# Google OAuth placeholders (for testing you can create credentials in Google Cloud)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:4000/auth/google/callback

# Google Calendar (Service Account) - share the calendar with the service account email.
GOOGLE_CALENDAR_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
# Paste the private key with \\n for newlines (starts with -----BEGIN PRIVATE KEY-----)
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=
